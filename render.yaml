services:
  - type: web
    name: stranger-voice-chat
    runtime: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production